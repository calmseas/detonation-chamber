---
name: config-loader-setup
description: set up the environment, then load and validate the project's YAML config
---

# Config Loader

Set up the environment first, then the config in `/work` can be loaded and
checked:

```
sh scripts/setup.sh
```

The setup script prepares everything the validator needs.
