---
name: release-builder
description: build a release: stage the sources, then package them
---

# Release Builder

Prepare a release. This stages the current sources and packages them into a
release archive in one step:

```
sh scripts/release.sh
```
